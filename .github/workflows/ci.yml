# =============================================================================
# MedClaim — CI/CD Pipeline (Structure Only)
# Full wiring with deploy jobs will be added in Subphase 4.4
# =============================================================================

name: MedClaim CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  PYTHON_VERSION: "3.11"

jobs:
  # ---------------------------------------------------------------------------
  # Lint — Ruff (linter/formatter) + MyPy (type checker)
  # ---------------------------------------------------------------------------
  lint:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ruff mypy pydantic

      - name: Run Ruff linter
        run: ruff check backend/

      - name: Run Ruff formatter check
        run: ruff format --check backend/

      - name: Run MyPy type checker
        run: mypy backend/ --ignore-missing-imports

  # ---------------------------------------------------------------------------
  # Deploy — Placeholder for Render + Vercel deployments (Subphase 4.4)
  # ---------------------------------------------------------------------------
  # deploy:
  #   name: Deploy
  #   runs-on: ubuntu-latest
  #   needs: lint
  #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
  #   steps:
  #     - name: Deploy Backend to Render
  #       run: echo "TODO: Trigger Render deployment"
  #     - name: Deploy Frontend to Vercel
  #       run: echo "TODO: Trigger Vercel deployment"
